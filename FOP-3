#include<stdio.h>
void main()
{
    int num,i,mul;
    printf("Enter Number\n");
    scanf("%d",&num);
    for(i=1;i<=10;i++)
    {
        mul=num*i;
        printf("%d * %d = %d\n",num,i,mul); 
    }
}
